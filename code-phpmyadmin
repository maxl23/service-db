_create database userdb:
  CREATE DATABASE userdb

_create table users
  CREATE TABLE users(
      id int AUTO_INCREMENT PRIMARY KEY;
      username varchar(255);
      email varchar(255);
      password varchar(255);
);
